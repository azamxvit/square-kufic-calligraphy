# Square Kufic Calligraphy — Interactive Canvas Generator

Этот проект визуализирует **куфическую каллиграфию** (Square Kufic) в интерактивном виде с помощью **JavaScript**, **Canvas API** и **CSS 3D-трансформаций**.

---

## 📜 Описание

Функция `solution(name, length, w, r, letters)` создаёт динамическую 3D-композицию из символов (или узоров), используя данные о форме каждой буквы.

Каждая буква визуализируется в отдельном `.wrapper` элементе с несколькими слоями (`canvas`), которые можно вращать и изменять, создавая эффект каллиграфической композиции в пространстве.

---

## ⚙️ Параметры функции

```js
solution(name, length, w, r, letters)
